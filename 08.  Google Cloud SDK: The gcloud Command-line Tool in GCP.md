![](https://media2.dev.to/dynamic/image/width=1000,height=420,fit=cover,gravity=auto,format=auto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2Fxnj9s3bpjt9xzo6tc5s5.png)

**Google Cloud SDK: Công cụ dòng lệnh gcloud trong Google Cloud Platform (GCP)**
**Định hướng:** Google Cloud Associate Cloud Engineer

---

## 1. Tổng quan

Khi làm việc với **Google Cloud Platform (GCP)**, người dùng không bắt buộc phải thao tác hoàn toàn thông qua giao diện đồ họa của **Google Cloud Console**. Thay vào đó, có thể sử dụng **gcloud** – công cụ dòng lệnh thuộc **Google Cloud SDK** – để quản lý, cấu hình và tự động hóa hầu hết các dịch vụ trên GCP.

Công cụ này đặc biệt quan trọng trong các hoạt động vận hành, triển khai hệ thống, tự động hóa quy trình và tích hợp CI/CD.

---

## 2. gcloud CLI là gì?

**gcloud CLI** (Google Cloud Command Line Interface) là giao diện dòng lệnh chính thức của Google dùng để tương tác với các dịch vụ Google Cloud.

Thông qua gcloud, người dùng có thể:

* Tạo, cấu hình và quản lý tài nguyên đám mây
* Triển khai ứng dụng
* Quản lý hạ tầng
* Tự động hóa các tác vụ vận hành

gcloud cung cấp một phương thức **nhất quán, chuẩn hóa và có thể lập trình** để làm việc với GCP.

---

## 3. Các dịch vụ có thể quản lý bằng gcloud CLI

gcloud hỗ trợ quản lý phần lớn các dịch vụ cốt lõi của GCP, bao gồm nhưng không giới hạn:

* **💻 Compute Engine**
  Tạo, cấu hình và quản lý máy ảo (VM instances)

* **🗄️ Cloud SQL**
  Quản lý cơ sở dữ liệu quan hệ (MySQL, PostgreSQL, SQL Server)

* **⚡ Cloud Functions & Cloud Run**
  Triển khai và quản lý ứng dụng serverless

* **☸️ Google Kubernetes Engine (GKE)**
  Quản lý cụm Kubernetes và các thành phần liên quan

* **🔥 Dataproc**
  Tạo và quản lý các cụm Hadoop và Apache Spark

* **🌍 Cloud DNS**
  Quản lý DNS zone và bản ghi DNS

* **🛠️ Cloud Deployment Manager**
  Triển khai và quản lý hạ tầng theo mô hình Infrastructure as Code (IaC)

**Kết luận:** Hầu hết các dịch vụ trên GCP đều có thể được quản lý thông qua gcloud CLI.

---

## 4. Các công cụ CLI khác trong hệ sinh thái GCP

Mặc dù gcloud là công cụ trung tâm, một số dịch vụ chuyên biệt vẫn có CLI riêng:

* **📊 Cloud Bigtable** → `cbt`
* **📈 BigQuery** → `bq`
* **📦 Cloud Storage** → `gsutil` (công cụ cũ nhưng vẫn được hỗ trợ)
* **☸️ Kubernetes** → `kubectl` (bắt buộc khi làm việc với Kubernetes)

**Lưu ý:**
Đối với **Cloud Storage**, Google hiện khuyến nghị sử dụng **gcloud** thay thế dần cho `gsutil` trong các kịch bản mới.

---

## 5. Các phương thức sử dụng gcloud CLI

### 5.1. Cài đặt gcloud trên máy cục bộ

Người dùng có thể cài đặt **Google Cloud SDK** trên máy tính cá nhân (Windows, macOS, Linux) để sử dụng gcloud.

Đặc điểm:

* Chủ động môi trường làm việc
* Dễ tích hợp với công cụ phát triển cục bộ
* Phù hợp cho phát triển dài hạn

---

### 5.2. Sử dụng gcloud trong Cloud Shell

Cloud Shell là môi trường dòng lệnh chạy trên trình duyệt, được Google cung cấp sẵn.

Đặc điểm:

* gcloud được cài đặt sẵn và luôn cập nhật
* Không cần cấu hình hay cài đặt
* Sẵn sàng sử dụng ngay khi mở Cloud Shell trong Google Cloud Console

Phù hợp cho:

* Học tập
* Thử nghiệm
* Quản trị và thao tác nhanh

---

## 6. Kết luận

**gcloud CLI** là công cụ không thể thiếu đối với **Associate Cloud Engineer**, lập trình viên và kỹ sư DevOps làm việc trên Google Cloud. Dù được cài đặt trên máy cục bộ hay sử dụng trực tiếp trong Cloud Shell, gcloud giúp:

* Quản lý tài nguyên đám mây hiệu quả
* Tự động hóa tác vụ vận hành
* Tích hợp GCP vào quy trình CI/CD
* Nâng cao năng suất và tính nhất quán trong quản trị hệ thống

Việc nắm vững gcloud CLI là một yêu cầu nền tảng trong lộ trình làm chủ Google Cloud Platform.
