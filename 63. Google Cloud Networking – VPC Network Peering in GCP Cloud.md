**Phần 63: Google Cloud Networking – VPC Network Peering trong GCP**


![](https://media2.dev.to/dynamic/image/width=1000,height=420,fit=cover,gravity=auto,format=auto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2Fty1jwsh9krcx4zl9qg4j.gif)
#google
#vpc
#cloud
#learning

Khi làm việc với Google Cloud, đôi khi bạn cần các tài nguyên trong **những mạng VPC khác nhau** có thể giao tiếp với nhau **một cách an toàn** mà không cần sử dụng **public IP**.

Đó chính là lúc **VPC Network Peering** được sử dụng.

---

# 🔹 VPC Network Peering là gì?

![](https://media2.dev.to/dynamic/image/width=800%2Cheight=%2Cfit=scale-down%2Cgravity=auto%2Cformat=auto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2Fh7dymzc2a1mgdjvhqgz2.png)

**VPC Peering** kết nối **hai mạng VPC** để các tài nguyên trong mỗi mạng có thể **giao tiếp với nhau thông qua địa chỉ IP nội bộ (private IP)**.

Có thể hình dung nó như việc tạo một **“cây cầu” giữa hai mạng**, trong khi **vẫn giữ chúng tách biệt**.

Được hỗ trợ trong các trường hợp như:

* **Cùng một project**
* **Các project khác nhau** (trong cùng một tổ chức)
* Thậm chí **giữa các tổ chức khác nhau** (các kịch bản đối tác hoặc khách hàng)

---

# Có thể sử dụng VPC Peering ở đâu?

**Compute Engine VMs**

→ Giao tiếp riêng tư giữa các VPC.

**Google Kubernetes Engine (GKE) clusters**

→ Chia sẻ dịch vụ và tài nguyên giữa các cluster.

**App Engine Flexible**

→ Truy cập các dịch vụ backend trong một VPC khác.

**Nhà cung cấp SaaS**

→ Cung cấp dịch vụ một cách an toàn cho khách hàng thông qua **Cloud Marketplace**.

---

# 🔹 Lợi ích

* **Độ trễ thấp**, kết nối riêng tư (không đi qua internet công cộng)
* Hoạt động với cả **IPv4 và IPv6**
* **Được quản lý hoàn toàn**, không cần cấu hình **VPN hoặc proxy**

---

# 🔹 Các điểm quan trọng

❌ **Hai VPC auto-mode không thể peering với nhau**
(vì chúng đều sử dụng dải IP **10.128.0.0/9** và sẽ bị trùng).

✅ **VPC auto-mode có thể peering với VPC custom-mode**
(miễn là **không có sự trùng lặp IP**).

**Dải CIDR của hai VPC phải khác nhau** (không được trùng lặp).

👉 **Ví dụ:**

* **VPC A → 10.10.0.0/16**
* **VPC B → 10.20.0.0/16**

Hai VPC này **có thể peering thành công**.

Nhưng nếu cả hai đều dùng **10.10.0.0/16**, thì **peering sẽ thất bại**.

---

# 🔹 Các hạn chế

VPC Peering rất mạnh mẽ, nhưng vẫn có một số giới hạn:

* Không cho phép **trùng lặp IP subnet**
* Không hỗ trợ **legacy networks**
* **Compute Engine DNS resolution** không hoạt động giữa các mạng peered
* **Tags và service accounts** dùng cho **firewall rules** không được truyền sang VPC khác
* **GKE** cần **IP Aliases** hoặc **custom routes** để hoạt động đầy đủ
* **Cloud Load Balancing** không thể có **frontend ở một VPC và backend ở VPC khác**

---

# ✅ Tóm tắt

**VPC Network Peering** = Giao tiếp **an toàn, riêng tư và độ trễ thấp** giữa các VPC.

Hoạt động **giữa nhiều project**, thậm chí **giữa nhiều tổ chức khác nhau**
(phù hợp cho các mô hình **SaaS hoặc shared services**).

Luôn **lập kế hoạch dải IP không trùng lặp** trước khi thiết lập peering.

Không phải tất cả các tính năng của Google Cloud đều hoạt động hoàn toàn giữa các mạng đã peering, vì vậy cần **kiểm tra các hạn chế trước khi triển khai**.
